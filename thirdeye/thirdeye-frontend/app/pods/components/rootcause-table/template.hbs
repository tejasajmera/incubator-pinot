{{!-- Uses a custom component that extends ember-models-table to handle cell selection --}}
{{models-table
  data=data
  columns=columns
  preselectedItems=preselectedItems
  showColumnsDropdown=false
  showGlobalFilter=false
  filteringIgnoreCase=true
  multipleExpand=true
  multipleSelect=true}}
